(function(){
  'use strict';
  var ctx=window.EdabalansAppContext||{}, identity=window.EdabalansIdentity||{}, email=identity.email||'', sessionToken=identity.sessionToken||'';
  var API=/^(localhost|127\.0\.0\.1|app\.edabalans\.ru)$/.test(location.hostname)?location.origin:'https://api.edabalans.ru';
  if(!document.getElementById('edabalans-masterclass-css')){var css=document.createElement('link');css.id='edabalans-masterclass-css';css.rel='stylesheet';css.href=(/^(localhost|127\.0\.0\.1)$/.test(location.hostname)?location.origin:'https://app.edabalans.ru')+'/assets/masterclass.css';document.head.appendChild(css);}
  function root(){return document.getElementById(ctx.app+'-app');}
  function esc(v){return String(v||'').replace(/[&<>"']/g,function(c){return {'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c];});}
  async function api(path,options){options=options||{};options.headers=options.headers||{};if(sessionToken)options.headers.Authorization='Bearer '+sessionToken;var r=await fetch(API+path,options);var j=await r.json();if(r.status===401&&sessionToken){try{localStorage.removeItem('edabalans_identity_v1');}catch(e){}throw new Error('Сессия входа закончилась. Обновите страницу и подтвердите email ещё раз.');}if(!r.ok)throw new Error(j.detail||j.error||'Ошибка сервера');return j;}
  function layout(title,lead,body){root().innerHTML='<main class="mc"><h1>'+esc(title)+'</h1><p class="lead">'+esc(lead)+'</p>'+body+'</main>';}
  function fail(e){layout('Не удалось открыть приложение',String(e.message||e),'<section class="mc-card mc-error">Если ошибка повторится, напишите Сергею: <b>@FitnessSergey</b></section>');}
  function questionnaire(kind){api('/api/masterclass/questionnaires/'+kind+'?email='+encodeURIComponent(email)).then(function(data){
    var title=kind==='onboarding'?'Небольшая анкета перед началом':'Саморевью по итогам мастер-класса';
    var lead=kind==='onboarding'?'Отвечайте в свободной форме. Каждый ответ сохраняется отдельно — можно закрыть страницу и продолжить позже.':'Соберите мысли в свободной форме. Не нужно повторять то, что уже подробно написано в дневнике.';
    layout(title,lead,data.questions.map(function(q,i){return '<section class="mc-card"><h2>'+(i+1)+'. '+esc(q.title)+'</h2><p>'+esc(q.prompt)+'</p><textarea data-code="'+esc(q.code)+'">'+esc(q.answer)+'</textarea><div class="save">Сохранится автоматически</div></section>';}).join('')+'<div class="mc-actions"><button id="mc-submit">'+(kind==='onboarding'?'Продолжить':'Отправить Сергею')+'</button><button class="secondary" id="mc-skip">Вернуться позже</button></div><p class="save" id="mc-status"></p>');
    Array.prototype.forEach.call(root().querySelectorAll('textarea'),function(field){var timer;field.addEventListener('input',function(){var status=root().querySelector('#mc-status');status.textContent='Сохраняю…';clearTimeout(timer);timer=setTimeout(async function(){try{await api('/api/masterclass/questionnaires/'+kind+'/answer',{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:email,question_code:field.dataset.code,answer_text:field.value})});status.textContent='Ответы сохранены';}catch(e){status.textContent='Не удалось сохранить: '+e.message;}},500);});});
    async function saveAll(){var fields=root().querySelectorAll('textarea');await Promise.all(Array.prototype.map.call(fields,function(field){return api('/api/masterclass/questionnaires/'+kind+'/answer',{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:email,question_code:field.dataset.code,answer_text:field.value})});}));}
    root().querySelector('#mc-submit').onclick=async function(){try{await saveAll();await api('/api/masterclass/questionnaires/'+kind+'/submit',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:email})});if(kind==='onboarding'){try{var link=await api('/api/masterclass/messenger-links',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:email,platform:'telegram'})});layout('Анкета сохранена','Ответы уже находятся в вашей карточке и не потеряются.','<section class="mc-card"><h2>Подключите Telegram</h2><p>Это остановит рекламу мастер-класса и позволит получать напоминания по программе.</p><a class="button" href="'+esc(link.deep_link)+'" target="_blank" rel="noopener">Перейти в Telegram</a><p class="save">Ссылка действует 15 минут. Она уже создана; обработка привязки на стороне бота пока подключается.</p></section>');}catch(linkError){layout('Анкета сохранена','Ответы уже находятся в вашей карточке и не потеряются.','<section class="mc-card mc-error"><h2>Telegram пока не подключён</h2><p>Ссылка ещё не готова. Вы сможете вернуться к этому шагу позже.</p></section>');}}else{layout('Сохранено','Ответы останутся в вашей карточке и не потеряются.','<section class="mc-card">Сергей получит уведомление после подключения служебного Telegram-чата.</section>');}}catch(e){fail(e);}};
    root().querySelector('#mc-skip').onclick=async function(){try{await saveAll();await api('/api/masterclass/questionnaires/'+kind+'/skip',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:email})});layout('Черновик сохранён','Вы сможете вернуться и продолжить с этого места.','');}catch(e){fail(e);}};
  }).catch(fail);}
  function countdown(value,node){if(!value){node.textContent='Действуют стандартные цены';return;}var end=new Date(value),exact=end.toLocaleString('ru-RU',{day:'2-digit',month:'2-digit',year:'numeric',hour:'2-digit',minute:'2-digit'});function tick(){var left=end-new Date();if(left<=0){node.textContent='Срок предложения завершён';return;}var h=Math.floor(left/36e5),m=Math.floor(left%36e5/6e4);node.textContent='Цена действует до '+exact+' · осталось '+h+' ч '+m+' мин';}tick();setInterval(tick,60000);}
  function ownedLabel(codes){var names=[],map={ACCESS_MASTERCLASS:'Мастер-класс',ACCESS_RECIPES:'Система рецептов',ACCESS_CALORIES:'«Калорийный»',ACCESS_STRENGTH:'«С дивана до тренировок»',ACCESS_CONSULTATION:'Индивидуальная консультация',ACCESS_CONSULTATION_RECORDINGS:'Записи консультаций'};codes.forEach(function(code){if(map[code])names.push(map[code]);});return names.length?names.join(' · '):'Мастер-класс';}
  function detailView(card,back){layout(card.title,card.description,'<div class="mc-detail-head"><button id="mc-back">← Вернуться к выбору и оплате</button></div><div class="mc-details">'+(card.details||[]).map(function(d){return '<section class="mc-card"><h2>'+esc(d.name)+'</h2><p>'+esc(d.description)+'</p></section>';}).join('')+'</div>');root().querySelector('#mc-back').onclick=back;}
  function offerCards(data,heading){function draw(){var owned='<section class="mc-owned"><b>У вас уже есть</b><span>'+esc(ownedLabel(data.owned_resources||[]))+'</span></section>';var cards=data.offers.length?data.offers.map(function(o,i){return '<section class="mc-card"><h2>'+esc(o.title)+'</h2><p>'+esc(o.description)+'</p><button class="link" data-detail="'+i+'">Подробнее</button><div class="offer-old">'+o.standard_price.toLocaleString('ru-RU')+' ₽</div><div class="offer-price">'+o.price.toLocaleString('ru-RU')+' ₽</div><div class="offer-saving">Выгода '+o.saving.toLocaleString('ru-RU')+' ₽ · '+o.saving_percent+'%</div><button data-offer="'+esc(o.code)+'">Купить</button></section>';}).join(''):'<section class="mc-card"><h2>У вас уже всё открыто</h2><p>Дополнительных предложений сейчас нет.</p></section>';layout(heading||'Дополнительные материалы','Здесь только актуальные варианты. Цена действует до указанного времени и повторно проверяется перед оплатой.',owned+'<div class="mc-countdown" id="mc-timer"></div><div class="offer-grid">'+cards+'</div>');countdown(data.expires_at,root().querySelector('#mc-timer'));Array.prototype.forEach.call(root().querySelectorAll('[data-detail]'),function(b){b.onclick=function(){detailView(data.offers[Number(b.dataset.detail)],draw);};});Array.prototype.forEach.call(root().querySelectorAll('[data-offer]'),function(b){b.onclick=async function(){try{var result=await api('/api/masterclass/checkout',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:email,placement:ctx.placement||'offers-hub',placement_token:ctx.placementToken||'',offer_code:b.dataset.offer})});location.hash=result.cart_command;}catch(e){fail(e);}};});}draw();}
  function offers(){api('/api/masterclass/offers?email='+encodeURIComponent(email)+'&placement='+encodeURIComponent(ctx.placement||'offers-hub')+'&placement_token='+encodeURIComponent(ctx.placementToken||'')).then(function(d){offerCards(d);}).catch(fail);}
  function gate(part){api('/api/masterclass/gate/'+part+'?email='+encodeURIComponent(email)+'&placement_token='+encodeURIComponent(ctx.placementToken||'')).then(function(d){if(d.allowed){layout('Доступ подтверждён','Этот раздел входит в ваш тариф.','<section class="mc-card mc-error"><h2>Техническая ошибка открытия</h2><p>'+esc(d.message)+'</p><p>Напишите Сергею: <b>'+esc(d.contact)+'</b></p></section>');}else{offerCards(d.offer,'Рецепты пока недоступны');}}).catch(fail);}
  if(ctx.app==='onboarding-questionnaire')questionnaire('onboarding');else if(ctx.app==='closing-review')questionnaire('closing-review');else if(ctx.app==='masterclass-offers')offers();else if(ctx.app==='recipes-part-1')gate(1);else if(ctx.app==='recipes-part-2')gate(2);
}());

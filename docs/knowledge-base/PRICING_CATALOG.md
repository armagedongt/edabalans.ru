# Единый каталог цен и тарифов

Статус: `current`
Состояние внедрения: код и админка подготовлены, использование
новым сайтом и checkout выключено до перепубликации
Владелец содержания: Сергей Воронцов
Проверено: 23.08.2026
Источники: `PRODUCTS.md`, `ACCESS_RULES.md`, `../TILDA_PAYMENTS.md`,
`../../backend/app/pricing_routes.py`, `../../backend/app/pricing_service.py`

## Назначение

Каталог является одним местом управления базовыми ценами продуктов, тремя
входными тарифами Мастер-класса и ступенями допродаж. Сайт, Мастер-класс,
Telegram, MAX и будущие каналы не должны хранить собственные копии цен.

Живым источником точных сумм является опубликованная версия в PostgreSQL. В
`PRODUCTS.md` хранится понятная человеку продуктовая политика и согласованный
снимок цен. Если Сергей меняет не только число, но и смысл тарифа, состав или
логику ступени, одновременно обновляется `PRODUCTS.md`.

## Почему цены хранятся версиями

- Черновик можно менять без влияния на покупки.
- После публикации версия неизменяема.
- Следующее изменение создаёт новый черновик-копию.
- Checkout и платёж сохраняют ID версии и код строки цены.
- Старый платёж продолжает ссылаться на правило, действовавшее при создании
  checkout; новая цена не пересчитывает историю.

Тариф определяется кодом версии предложения, а сумма используется как
обязательная дополнительная проверка. Определять тариф только по сумме запрещено:
одинаковая сумма может относиться к разным скидкам и предложениям.

## Данные без лишних сущностей

Используются две таблицы:

- `pricing_versions` — черновик, активная и архивные версии;
- `price_entries` — все строки цен конкретной версии.

Строка цены содержит стабильный код, раздел, название, состав ресурсов, базовую,
зачёркнутую и фактическую цену, активность и порядок. Отдельная таблица не
создаётся под каждый пакет или ступень.

Покупка, право и режим открытия остаются отдельными сущностями: цена не заменяет
`payments`, `user_accesses` или `user_course_policies`.

## Разделы страницы администратора

Страница `/admin/pricing` показывает:

1. Три тарифа будущей главной страницы.
2. Базовые цены отдельных продуктов.
3. Все ступени допродаж и цены комплектов.
4. Статус: черновик, активная или архивная версия.
5. Историю версий и признак включения нового серверного каталога.

Сергей меняет базовую, зачёркнутую и продажную цену в черновике. Публикация делает
версию неизменяемой, но пока `PRICING_CATALOG_ENABLED=false`, даже опубликованная
версия не влияет на текущий сайт и действующие покупки.

## Новый сайт и Tilda

После переключения:

1. Новый серверный сайт получает три тарифа через `/api/pricing/site`.
2. Нажатие кнопки вызывает `/api/pricing/site/checkout` с кодом тарифа, но без
   клиентской цены.
3. Backend берёт цену и состав из активной версии и создаёт короткоживущий
   checkout.
4. Стандартная корзина Tilda принимает оплату.
5. Подтверждённый webhook содержит ID checkout и email покупателя.
6. Backend создаёт или находит пользователя, сверяет версию, сумму и срок,
   сохраняет платёж и выдаёт точные ресурсы тарифа.
7. Tilda регистрирует покупателя в одной общей группе личного кабинета; разные
   продуктовые группы для трёх новых тарифов не используются.

Без ID checkout сервер не определяет новый тариф по одной сумме. Неизвестная
покупка сохраняется для проверки, но не получает права догадкой.

## Условия будущего включения

- Сергей перепубликовал новую главную страницу с серверными карточками тарифов.
- Все три кнопки создают серверный checkout и открывают рабочую корзину Tilda.
- Все три товара Tilda направляют покупателя в одну согласованную группу.
- Выполнена тестовая покупка каждого тарифа с отдельными email.
- Проверены платёж, версия цены, состав прав и вход в общий кабинет.
- После проверки `PRICING_CATALOG_ENABLED` переключается в `true`.

До выполнения этих условий модуль остаётся подготовленным, но выключенным.

# Сведение завершённых потоков — 29.08.2026

Основа интеграции: `origin/main` на коммите `719dc7c`.

## Принято

- `bd624e7` — source-first handoff для полной пересборки бесплатного интенсива.
- `a6271eb` — рабочая система первой рецептурной части, десять черновиков и варианты визуала конструктора.
- `4abc822` — локальная расшифровка медиа и импорт экспортов Telegram-консультаций без помещения исходных персональных файлов в Git.
- `420184b` — два варианта прототипа главной страницы; перенесены из бесхозного `prototypes/` в `docs/prototypes/homepage-redesign/`.
- Из `a9145c6` выборочно перенесены четыре очищенных авторских транскрипта. Они размещены в `content/masterclass/reference/structured-transcripts/`, а два действующих материала получили ссылки на них.

## Уже было в `origin/main`

Повторно не переносились: многострочные материалы курса, первый видеосценарий, footer fix, калькулятор рецептов, единая анкета дня 2, Писарь до `719dc7c`, DQS, блокировки материалов, прямой плеер, messenger-link и действующая логика предложений/саморевью.

## Не выпускать сейчас

- `70c84d6` — серверный каталог контента `/admin/content` и миграция `20260829_0029`. Перед выпуском нужны свежий backup, проверка восстановления, изолированный `upgrade -> downgrade -> upgrade` и CI. Ветка сохранена: `codex/content-catalog-server`.
- Полный `a9145c6` — сырые длинные эфиры и Q&A не перенесены: сначала нужна очистка возможных персональных деталей и исправление ссылочного каталога без замены более новых материалов Мастер-класса.

## Не относится к проектному `main`

- `d4816a2` — персональный скрипт копирования фотоархива. Это локальная операционная утилита без владельца в архитектуре проекта; ветка сохранена, в `main` не переносилась.
- Универсальные `work/integration-handoff.md` из разных потоков не переносились: они конфликтуют по одному имени и не являются канонической документацией.

## Проверки

- `python -m pytest tests/test_local_media.py tests/test_telegram_consultations.py -q` из `backend/`: 9 passed.
- `python -m pytest tools/tests/test_module_inventory.py -q`: 16 passed, 3 subtests passed.
- `python tools/module_inventory.py --tracked-only`: карта собрана, бесхозных файлов нет.
- `git diff --check`: ошибок пробелов нет.

## Незавершённые решения владельца

- Выбрать вариант визуала конструктора питания и после этого утвердить граммовки/DQS рецептурной системы.
- Решить судьбу прототипа главной страницы после просмотра; production Tilda не переключалась.
- Отдельно санитизировать и принять либо закрыть сырые расшифровки из `a9145c6`.

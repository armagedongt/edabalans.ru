# Разбор общей рабочей папки — 18.09.2026

Статус: `draft`
Назначение: checkpoint ограниченного разбора, не канон и не разрешение удаления.

## Проверенная граница

Общая папка `C:/Users/Segey/Documents/ChatGPT/edabalans.ru` остаётся на
`codex/markiting`, HEAD `e204762`. Сравнение выполнено с полученным
`origin/main` `0784833`. Staged-файлов в момент проверки нет. Новая запись
в общий checkout не выполнялась; очистка index, reset, stash и switch запрещены.
Корень не является актуальным checkout main, даже если отдельные изменения в нём
уже приняты. Сравнение Git-blobs учитывает штатную нормализацию конца строк.

[Снимок сравнения](root-reconciliation-20260918.json) содержит только пути и
классификацию: без текста, секретов, медицинских или клиентских данных.
Не проверялись приватные/ignored-файлы, содержимое исходных медиа и вложенные
worktree. Снимок не обновляется автоматически и не заменяет повторную проверку
перед записью или уборкой.

## Результат сравнения

| Проверенный набор | Совпадает с main | Отличается | Только локальный путь | Удалено локально, есть в main |
|---|---:|---:|---:|---:|
| 107 путей реального diff HEAD | 8 | 97 | 1 | 1 |
| 61 untracked-файл выбранных кодовых/нормативных каталогов | 13 | 9 | 39 | 0 |

Это не счётчик всей незакоммиченной работы. `git status` может дополнительно
показывать строки без содержательного `git diff HEAD`; число строк статуса не
равно числу уникальных потерянных файлов. Совпадение файла с main не разрешает
удалять его из продолжающейся рабочей копии. Отличие не доказывает новое полезное
поведение: это может быть отставшая версия, смешанная правка или черновик.

У HEAD есть 43 коммита вне ancestry main. `git cherry` нашёл для 13 эквивалентные
patches; у остальных 30 patch-эквивалентность не подтверждена. Они не объявляются
30 потерянными функциями: более поздняя переработка/сведение может менять patch-id.
Ветки и историю сохранять до содержательной проверки конкретного контура.

## Что выделено по владельцам

- **Голосовой приём — platform.content.** Владелец задачи подтвердил готовность
  локального `VOICE_TRANSCRIPTION_WORKFLOW.md`, отсутствие новой версии и
  параллельной записи. Для приёмки берётся только нормативный документ и адресные
  маршруты. Raw/derived/private, исходные медиа и глобальный workflow не входят.
- **Писарь и дизайн.** Старые root-версии навыков/контрактов отличаются от main;
  не устанавливать их поверх глобальных навыков. Отсутствующие в старом корне
  современные файлы нельзя считать лишними или удалёнными по решению владельца.
- **Markdown-прототип.** Локальная `work/masterclass-obsidian-publishing/` не
  является потерянным production-renderer: существующий
  `content/article-components/DESIGN_HANDOFF.md` уже связывает сохранённый Git
  прототип `4e1bd91015945a6b7da4d359dc51a3abe7ed8f6c`. Отличающиеся локальные CSS,
  lab и исследования сохранять; перенос движка в main этой приёмкой не выполняется.
- **Калорийный курс — products.calories.** Root-версии service/routes/tests и
  COURSE_RUNTIME отличаются от main. Историческая локальная migration
  `20260828_0027_calorie_course_runtime.py` не переносится по одному наличию:
  версия текущей схемы и эквивалентная реализация проверяются отдельно.
- **Каталог контента — platform.content.** Local viewer/build/import tools
  остаются отдельным кандидатом сверки с действующим серверным каталогом;
  отсутствие пути в main не даёт права удалить его или объявить готовым.
- **Учебные сообщения и материалы — продуктовые владельцы + platform.content.**
  Смешанные тексты интенсива, МК, scripts и assets сохранять. Локально удалённый
  `intensive-masterclass-circle.md` не восстанавливать автоматически: он есть
  в main, но причина локального удаления этим проходом не установлена.
- **AI control/intake и личные производные.** Непринятые исследования и приватные
  результаты из прежних [отчётов задач](task-reports.md) не относятся к безопасной
  механической очистке и не включались в этот файловый набор.

## VSL и свободное место

Последний checkpoint задачи VSL сообщил о прерывании записи HTML при заполненном
C:. Production той новой правкой не затронут. Последующая проверка диска показала
463970304 байт свободно — около 442 МиБ, не гарантированный постоянный запас.
Задаче отправлен этот факт и требование проверить собственный HTML перед
возобновлением. Чат и его копии не архивировались и не очищались.

## Что можно делать дальше

1. Принять голосовой регламент отдельным узким пакетом поверх свежего main.
2. Согласовать владельцев следующих смешанных code/text-остатков и сверить их
   с текущими канонами: нельзя вливать всю голову codex/markiting.
3. Только после этой сверки и передачи очереди записи подготовить переход общей
   папки к последовательной работе. Сейчас её checkout не переключается.
4. Для освобождения диска подготовить точные свободные worktree/cache-цели,
   проверить private/ignored и получить отдельную команду удаления.

В этом проходе никакие ветки, worktree, файлы или задачи не удалялись. Общая
папка остаётся без изменений; факт публикации узкого нормативного пакета не
означает, что весь root разобран, обновлён или готов к массовой очистке.

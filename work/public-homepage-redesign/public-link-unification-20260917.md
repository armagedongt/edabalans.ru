# Постоянные публичные ссылки: checkpoint 17.09.2026

Статус документа: `current`. Состояние реализации: `in_development`.
Владелец основной задачи: `products.public-site`; общие footer, cookie, legal,
blog и intensive сохраняют существующих владельцев из `docs/modules.toml`.

## Запрос и границы

Публичные входы шапок/подвалов, новой главной и юридических документов направить
на `похудение-это-есть.рф`, временно открыть серверные страницы через Tilda.
Тексты, цены, права, персональные ссылки бота и source_context не менять.
API, ресурсы, внутренние auth-действия и внешние контакты не переводить на Tilda.

## Выполнено локально

Рабочая папка `D:/CodexWork/public-link-unification-20260917`, ветка
`codex/public-link-unification-20260917`, исходная точка `af52ed6`.
Исправлены shared footer/header/cookie, ссылки активной главной, связанных legal,
платёжных форм, навигация блога/интенсива и единый public intensive CTA.
Ресурсы остаются на сервере, локальные якоря сохранены.
114 выбранных backend-тестов прошли, включая HTTP-проверку legal-навигации.
Две волны code/test review нашли и помогли закрыть старое browser-ожидание и
пропущенные legal-ссылки native checkout и форм обязательных документов.
Legal versions/acceptances не меняются; friendly paths документов в metadata
и фиксированный публичный origin renderer заменяют прежние server-host anchors.

## Постоянные входы опубликованы

17.09.2026 вход восстановлен. Все четыре перехода созданы и опубликованы,
публичная HTTP-проверка подтверждает код сохранения query/fragment и ответ 200.

| Новый адрес Tilda | Фиксированная цель |
|---|---|
| `/legal` | `https://edabalans.ru/legal` |
| `/legal/messages` | `https://edabalans.ru/legal/messages` |
| `/legal/disclaimer` | `https://edabalans.ru/legal/disclaimer` |
| `/blog` | `https://blog.похудение-это-есть.рф/` |

Tilda page IDs: legal `250341509`, messages `250367809`, disclaimer `250369109`,
blog `250371209`. Наследованы noheader/nofooter/nosearch; папка «Главные входы».
В шапке `49187165` исправлена чужая cookie-ссылка на постоянный `/legal/privacy`.
В 404 `61927151` исправлен fallback `/lk`; обе страницы опубликованы.
Старый `/docs` и `/docs/` на app-host исправлены в Caddy на общий `/legal`
с query. Caddy adapt --validate прошёл в действующем production image без загрузки
нового конфига; узкий infrastructure review clean.
Пакет сведен поверх `39e1c04`, повторно прошли 114 backend-тестов.

Выпуск `c8ee743` включает этот пакет и успешно завершён на production.
Браузерная проверка старого `/docs` сохранила повторяющиеся query и fragment
и открыла общий Legal; `/blog` открыл существующий blog-host с query/fragment.
На 17 уже опубликованных страницах перепубликована копия исправленной шапки;
в `main-old` отдельно исправлен собственный cookie-блок. Старые `/oferta` и
`/privacy_policy` получили T123-переходы без удаления прежних блоков.
Финальная проверка обнаружила отсутствующий якорь `#masterclass`: внешняя
навигация блога/интенсива/shared header теперь ведёт на корень главной без него.

Блог использует существующий серверный blog-host: его статьи имеют пути
`/articles/*`; не направлять `/blog` на edabalans.ru/blog без изменения их маршрутизации.
Для каждого T123: фиксированный URL + `location.search` + `location.hash`, noscript,
noheader/nofooter/nosearch, папка «Главные входы», публикация только новой страницы.
Тексты legal не копировать в Tilda. Проверить уже существующие `/lk`, `/intensive`,
`/legal/offer`, `/legal/privacy`, `/legal/consent`, `/pay`, `/subscription`.

## После восстановления входа

1. Создать четыре перехода, проверить query/hash, без удаления старых блоков/страниц.
2. Проверить глобальную шапку `49187165` и footer `86576196`; footer уже использует
   shared loader, поэтому изменение его серверного источника обновит ссылки.
3. В Tilda404 `61927151`, T123 `3967495201` изменить только fallback LK на
   `https://похудение-это-есть.рф/lk`; script source не менять, старые блоки не удалять.
4. Дождаться fresh reviews, устранить только реальные дефекты, повторить проверки.
5. Поверх свежего main принять изолированный результат и штатно выпустить backend.
6. Проверить главную `221202909`, документы, header/footer/404 и реальные переходы.
   Контрольную покупку, отправку рассылок и переключение DNS не выполнять.

Главная уже назначена ранее; повторно её не переключать. Этот checkpoint — не план
и не новый источник правил сайта.

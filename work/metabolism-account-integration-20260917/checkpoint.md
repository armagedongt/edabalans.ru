# Интеграция метаболизма — рабочий checkpoint

Статус: `draft`

Реализация: `in_development`
Модуль: `products.metabolism`

Поручение 17.09.2026: перенести одобренный локальный калькулятор в реальный контур,
добавить цветные акценты/шкалу и два блока для ручных скриншотов, убрать экспорт,
сохранить старый на отдельном адресе, связать структурированные данные анкеты,
доступ разрешать после второго этапа курса о калориях.

Рабочая папка: `D:/CodexTools/edabalans-metabolism-account-20260917`.
Основа визуала — локальный commit `2282cd4`, не старый макет на 8812.
Канонический технический договор —
`docs/knowledge-base/modules/metabolism/APP_RUNTIME.md`.

Этапы: локальная реализация → API/browser-проверки → свежие reviews → сохранение
в Git → перенос только этого набора поверх свежего main → штатный deploy backend
→ проверка новых и старого адресов. Нужна миграция `20260918_0043`: прежний SQL CHECK
разрешает только active_variant 1/2; разрешаем 3 без переписывания JSON. До отдельного
подтверждения владельца main/deploy не выполнять. Массовая миграция ответов не нужна;
реальные значения не импортируются. Откат интерфейса возможен возвратом
прежней оболочки; пользовательские варианты остаются в той же таблице.

Чат «МК: полный прогон материалов» уведомлён и подтвердил hands off полей анкеты.
Редактура остальных материалов независима. Auth release другого потока завершён,
сессии/пароли этим изменением не затрагиваются.

## Проверки и приёмка

- Полный backend после изменения схемы и сведения свежего main: 557 passed,
  3 skipped; 59 затронутых тестов passed. Browser metabolism и embed-loading
  повторно passed после последних исправлений.
- Свежая волна 1 нашла отсутствие двух assets в allowlist и два рассогласования
  процентного дефицита. Исправлены; добавлены прямые проверки настоящих routes,
  reload процентов, пустого поля и сохранения из старого интерфейса.
- Новый флаг прохождения больше не раскрывается анонимному legacy API или чужой
  сессии; существующие auth/session алгоритмы не менялись.
- Дополнены проверки изоляции профилей двух пользователей и PUT до этапа 2.
- Визуальная база: 9 ширин и расширенные состояния в приватной evidence-папке D:.
- Точный upgrade/downgrade миграции проверен на изолированной временной таблице
  PostgreSQL в откатываемой транзакции. Постоянная таблица, её CHECK и записи
  не изменялись. Alembic имеет единственную голову `20260918_0043`.
- Обновлённая основа включает origin/main `0784833`, не только `eed6a87`.
- Волна 2 security: доказанных нарушений нет. Code/test review выявили сохранение
  устаревшего эквивалента ккал у процентного дефицита и реальный архивный payload,
  сохранявший процентный режим после редактирования ккал. Исправлены snapshot и
  архивный редактор; browser-тест использует настоящий старый интерфейс, возвращается
  в новый и проверяет режим/значение. Добавлена проверка изменения общего веса
  во всех трёх сохраняемых вариантах.
- Исправлена только тестовая сборка скриншотов анкеты: обе оболочки теперь имеют
  доказательства на всех девяти фактических ширинах, а не четырёх повторных.

До production остаются завершение визуальной проверки и итоговая фиксация ветки,
подтверждение владельца на миграцию, backup + проверка восстановления, CI целевого
коммита и защищённый ручной выпуск. Main и production этой задачей не изменены.

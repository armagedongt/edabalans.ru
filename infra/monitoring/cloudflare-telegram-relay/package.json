{
  "name": "edabalans-cloudflare-telegram-relay",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "deploy": "node scripts/deploy.mjs",
    "check": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "4.129.0"
  }
}

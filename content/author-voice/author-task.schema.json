{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://edabalans.ru/schemas/author-task-v2.json",
  "title": "Edabalans author task",
  "type": "object",
  "required": ["note", "work_profile", "edit_mode", "source_basis"],
  "properties": {
    "note": {"type": "string", "minLength": 1},
    "work_profile": {
      "enum": ["structure", "transcript_to_article", "new_material", "develop_existing"]
    },
    "source_basis": {"enum": ["full_source", "sparse_basis"]},
    "transcript_role": {
      "enum": ["article_source", "video_script", "context_only"]
    },
    "transcript_context": {"type": "string", "minLength": 1},
    "edit_mode": {
      "enum": ["draft", "targeted_edit", "proofread", "structure_only", "text_only", "rewrite", "creative_rebuild"]
    },
    "surface_context": {
      "enum": [
        "article", "site_article", "telegram", "telegram_channel",
        "pikabu", "pikabu_article", "landing", "service_text", "bot_sequence",
        "course", "course_material", "course_package", "masterclass_course",
        "masterclass_material", "intensive_article"
      ]
    },
    "format_profile": {"enum": ["article", "course"]},
    "course_context": {
      "type": "object",
      "required": ["day_context", "material_role", "continuity"],
      "properties": {
        "day_context": {"type": "string", "minLength": 1},
        "material_role": {"type": "string", "minLength": 1},
        "continuity": {"type": "string", "minLength": 1}
      },
      "additionalProperties": false
    },
    "course_continuity": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["idea", "route"],
        "properties": {
          "idea": {"type": "string", "minLength": 1},
          "route": {"type": "string", "minLength": 1}
        },
        "additionalProperties": false
      }
    },
    "source_text": {"type": "string"},
    "rewrite_goal": {"type": "string", "minLength": 1},
    "editable_scope": {
      "type": "array",
      "minItems": 1,
      "items": {
        "oneOf": [
          {"type": "string", "minLength": 1},
          {
            "type": "object",
            "required": ["source"],
            "properties": {"source": {"type": "string", "minLength": 1}},
            "additionalProperties": true
          }
        ]
      }
    },
    "preservation_anchors": {
      "type": "array",
      "items": {"type": "string", "minLength": 1},
      "uniqueItems": true
    },
    "allowed_removals": {
      "type": "array",
      "items": {"type": "string", "minLength": 1}
    },
    "structural_labels": {
      "type": "array",
      "items": {"type": "string", "minLength": 1},
      "uniqueItems": true
    },
    "min_token_coverage": {"type": "number", "minimum": 0, "maximum": 1},
    "min_length_ratio": {"type": "number", "minimum": 0, "maximum": 1},
    "fact_sources": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["name", "fingerprint"],
        "properties": {
          "name": {"type": "string", "minLength": 1},
          "fingerprint": {"type": "string", "minLength": 1}
        },
        "additionalProperties": false
      }
    },
    "fact_check_profile": {
      "enum": ["instructional_strict", "editorial_materiality"]
    },
    "required_facts": {
      "type": "array",
      "items": {
        "oneOf": [
          {"type": "string", "minLength": 1},
          {
            "type": "object",
            "required": ["text"],
            "properties": {
              "text": {"type": "string", "minLength": 1},
              "mode": {"enum": ["semantic", "verbatim"]}
            },
            "additionalProperties": false
          }
        ]
      }
    },
    "forbidden_claims": {
      "type": "array",
      "items": {"type": "string", "minLength": 1},
      "uniqueItems": true
    },
    "article_components": {
      "type": "array",
      "items": {"enum": ["slider", "dqs_score_table", "spoiler"]},
      "uniqueItems": true
    },
    "legacy_article_migration": {"type": "boolean"},
    "visual_contract_change": {"type": "boolean"}
  },
  "allOf": [
    {
      "if": {"properties": {"work_profile": {"const": "structure"}}},
      "then": {"properties": {"edit_mode": {"const": "structure_only"}}, "required": ["source_text"]}
    },
    {
      "if": {"properties": {"work_profile": {"const": "transcript_to_article"}}},
      "then": {
        "properties": {
          "edit_mode": {"const": "rewrite"},
          "source_basis": {"const": "full_source"},
          "transcript_role": {"enum": ["article_source", "video_script"]},
          "preservation_anchors": {"minItems": 1}
        },
        "required": ["source_text", "transcript_role", "preservation_anchors", "rewrite_goal"]
      }
    },
    {
      "if": {
        "properties": {"transcript_role": {"const": "context_only"}},
        "required": ["transcript_role"]
      },
      "then": {
        "properties": {
          "work_profile": {"enum": ["new_material", "develop_existing"]}
        },
        "required": ["transcript_context"]
      }
    },
    {
      "if": {"properties": {"work_profile": {"const": "new_material"}}},
      "then": {"properties": {"edit_mode": {"enum": ["draft", "creative_rebuild"]}}}
    },
    {
      "if": {
        "properties": {"source_basis": {"const": "sparse_basis"}},
        "required": ["source_basis"]
      },
      "then": {
        "properties": {
          "work_profile": {"const": "new_material"},
          "edit_mode": {"enum": ["draft", "creative_rebuild"]}
        }
      }
    },
    {
      "if": {
        "properties": {
          "source_basis": {"const": "full_source"},
          "work_profile": {"const": "new_material"}
        },
        "required": ["source_basis", "work_profile"]
      },
      "then": {
        "properties": {
          "edit_mode": {"const": "creative_rebuild"},
          "preservation_anchors": {"minItems": 1}
        },
        "required": ["source_text", "preservation_anchors"]
      }
    },
    {
      "if": {"properties": {"work_profile": {"const": "develop_existing"}}},
      "then": {
        "properties": {
          "edit_mode": {"enum": ["rewrite", "targeted_edit", "proofread", "text_only"]},
          "preservation_anchors": {"minItems": 1}
        },
        "required": ["source_text", "preservation_anchors"]
      }
    },
    {
      "if": {
        "properties": {
          "work_profile": {"const": "develop_existing"},
          "edit_mode": {"const": "rewrite"}
        },
        "required": ["work_profile", "edit_mode"]
      },
      "then": {"required": ["rewrite_goal"]}
    },
    {
      "if": {
        "properties": {
          "work_profile": {"const": "develop_existing"},
          "edit_mode": {"const": "targeted_edit"}
        },
        "required": ["work_profile", "edit_mode"]
      },
      "then": {"required": ["editable_scope"]}
    },
    {
      "if": {
        "properties": {"required_facts": {"minItems": 1}},
        "required": ["required_facts"]
      },
      "then": {
        "properties": {"fact_sources": {"minItems": 1}},
        "required": ["fact_sources"]
      }
    },
    {
      "if": {
        "properties": {
          "surface_context": {
            "enum": ["course_material", "masterclass_material", "intensive_article"]
          }
        },
        "required": ["surface_context"]
      },
      "then": {"required": ["course_context"]}
    },
    {
      "if": {
        "properties": {"format_profile": {"const": "course"}},
        "required": ["format_profile"]
      },
      "then": {"required": ["course_continuity"]}
    }
  ],
  "additionalProperties": true
}

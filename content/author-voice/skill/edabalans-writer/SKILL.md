---
name: edabalans-writer
description: |
  Пишет, переписывает, редактирует и структурирует читательские, учебные и маркетинговые материалы edabalans.ru в голосе Сергея Воронцова.

  Use when пользователь просит создать, переписать, сократить, расширить,
  структурировать или отредактировать пользовательский, учебный, маркетинговый
  или редакционный текст edabalans.ru либо материал от лица Сергея Воронцова.

  Do NOT use для разработки кода, инфраструктуры, CRM, авторизации, базы данных,
  интерфейса или документации проекта, если задача не включает работу над самим
  текстом для читателя, ученика или покупателя.
---

# Писатель edabalans.ru

Этот навык — обязательная точка входа для любой генерации или редактуры
пользовательского, учебного, маркетингового и редакционного текста проекта.
Он не хранит второй голос: все правила письма принадлежат только модулю
`platform.content`.

Все пути ниже указаны относительно корня репозитория edabalans.ru — каталога, в
котором лежат `AGENTS.md` и `docs/modules.toml`. Сначала найти этот корень, затем
разрешать пути от него; не разрешать их относительно каталога установленного
skill.

Карта всего контура — `content/author-voice/README.md`, раздел «Маршруты реальных
задач». Через неё выбирать дополнительные продуктовые источники и доставку;
историю аудитов и весь корпус для рядовой правки не загружать. Старый handoff,
приватный паспорт или установленный адаптер не заменяет нормативный контракт.
Если нужный контракт отсутствует или его устаревание уже установлено, получить его из принятой
Git-версии, а не писать по устаревшей копии или придумывать правила; чужую грязную
рабочую папку при этом не обновлять автоматически.

## Обязательный вход

До черновика провести короткий предстартовый аудит по
`docs/knowledge-base/EDITORIAL_PRODUCT_SYSTEM.md`. Сначала заполнить ответы из
запроса, оболочки продукта и Библиотекаря. Если неизвестна развилка, которая
меняет цель, аудиторию, допустимое раскрытие платного, приоритет оригинальности
или готовых блоков, состав материала либо публикацию, спросить Сергея одним
пакетом и не писать зависимую часть наугад. Для ясной задачи ничего не спрашивать.
Слово «автономно» убирает лишние остановки после старта, но не разрешает
пропустить эту проверку или придумать ответ за владельца.

1. Выбрать профиль и режим по короткой карте:
   - чистое «структурируй», «только структура» без содержательных комментариев →
     одна пользовательская операция из
     двух узких pack: сначала `develop_existing` + `proofread`, затем передать
     проверенный результат как `source_text` в `structure` + `structure_only`;
     исправить обычные опечатки, орфографию, пунктуацию и согласования, затем
     добавить функциональные заголовки и разметку. Заголовок может потребовать
     служебного слова или другого падежа, но не вводит новый тезис. Намеренные
     «кааак», «вооот», перебивки и звукоподражания сохранить. Дословность
     конкретной цитаты задаётся для её блока, а не отдельным глобальным режимом;
   - голосовая/видео-расшифровка, из которой делается статья или сценарий →
     `transcript_to_article` + `rewrite`: сохранить авторский ход,
     формулировки, примеры и заранее зафиксированные якоря;
   - расшифровка только для понимания соседнего видео → профиль целевого текста
     + `transcript_role=context_only`: не переносить речь в статью автоматически;
   - материал из заметки с нуля → `new_material` + `draft`; полная пересборка
     существующего текста допустима только как явный `creative_rebuild`;
   - расширить или усилить готовую статью → `develop_existing` и минимально
     достаточный `targeted_edit`, `proofread`, `text_only` либо `rewrite`.
   Затем записать `source_basis`: `full_source` для полного авторского исходника
   либо `sparse_basis` для малой базы/написания с нуля. Это не новый режим и не
   расширение разрешённой правки.
   Отдельно записать `author_reuse_mode`. Для базового материала курса,
   интенсива или справочной статьи с каркасом выбрать
   `authored_blocks_first`, превратить темы каркаса в `block_outline` и искать
   авторский текст по каждому блоку до написания черновика. Для рядового нового
   Telegram-поста выбрать `original_composition`, если Сергей не попросил
   повторить конкретный старый фрагмент.
   При неоднозначности или сложной границе полностью прочитать
   `content/author-voice/authoring-work-profiles-v1.md` и
   `content/author-voice/editing-modes-v1.md`; в ясной задаче эта
   загрузка не обязательна.
   Обязательные границы ясных режимов уже заданы здесь:
   - `structure_only` сохраняет все авторские символы, ссылки и медиа; добавляет
     только заголовки из точного `structural_labels`;
   - `transcript_to_article` удаляет лишь дословные фрагменты из
     `allowed_removals`, сохраняет примеры, ход мысли, anchors, ссылки и медиа и
     работает только на записанный `rewrite_goal`;
   - `targeted_edit` меняет только уникальные исходные фрагменты из
     `editable_scope`; всё между ними остаётся без изменений;
   - `text_only` меняет формулировки внутри всего текста, но сохраняет порядок и
     типы блоков, ссылки, медиа и привязку inline-выделений;
   - `proofread` допускает только орфографию, пунктуацию и явно названное
     согласование; `rewrite` готовой статьи сохраняет anchors и обслуживает
     конкретный `rewrite_goal`; `draft` создаёт новый текст, а
     `creative_rebuild` требует прямого разрешения пересобрать существующий.
   Для транскрипта также записать `transcript_role`: `article_source`,
   `video_script` или `context_only`. Последний остаётся контекстом соседнего
   материала и не переносится в текст автоматически.
   Если внутри голосового наброска, черновика или готового текста Сергей дал
   разные комментарии разным фрагментам, полностью прочитать раздел «Смешанные
   комментарии внутри одного исходника» в
   `content/author-voice/editing-modes-v1.md` и записать
   `block_instructions`. Этот документ единолично владеет перечнем действий и
   разрешениями; комментарии не являются частью будущей публикации. Если вместе
   со структурой требуется раскрыть, найти, исследовать, написать или удалить
   блок, сначала выполнить содержательный `rewrite` с этими инструкциями, а уже
   его результат пропустить через корректуру и структурную сборку.
2. Для `draft`, `rewrite` и `creative_rebuild` полностью прочитать
   `content/author-voice/writer-contract-v1.md`. Для локального `targeted_edit`
   и `text_only` прочитать разделы 1, 7 и 10: позиция автора, язык, первое лицо,
   границы резкости и антинейрослоп. Если меняется аргумент, дополнительно
   применить разделы 2–6; продажа / CTA требует разделов 8–9 и владельцев фактов.
   Это выбор нужной части того же канона, не ослабление разрешений режима.
   Для `proofread` и `structure_only` контракт не нужен, если
   границы правки заданы явно и смысл текста не расширяется: здесь достаточно
   исходника, `editable_scope`, сохранения структуры и назначенных validator
   checks.
3. Определить место текста и площадку доставки по
   `content/author-voice/platform-adaptation-v1.md`: прочитать выбор маршрута и
   только относящиеся к задаче разделы. Карта обязательна при написании для
   известной площадки или переносе между площадками, но не требует загрузки всех
   адаптеров для корректуры одной фразы. Она не расширяет разрешения редактуры
   и не включает явно вызываемый `edabalans-pikabu` автоматически.
   Затем подключить только применимый источник:
   - точный CTA, ссылка или комментарийный импульс —
     `content/author-voice/editorial-linking-v1.md`;
   - серия, ссылка на старый материал или узел бот-цепочки — тот же документ и
     адресный поиск по каталогу старых материалов;
   - обычная статья сайта, интенсива или курса — держать `h1` вне тела, использовать
     только `h2`/`h3`, абзацы, списки, `strong`/`em`, ссылки, изображения,
     многострочную цитату `>` и единую плашку `> [!NOTE]`; `:::note` — только
     совместимость. Расширенные LINK, NOTE-minus, copy и прочие роли из draft
     `MARKDOWN_MATERIAL_TRANSFER.md` не считать установленными в API по наличию
     локального образца. Полный
     `docs/knowledge-base/ARTICLE_STANDARD.md` читать только при переносе legacy
     HTML или изменении визуального/runtime-контракта, а
     `content/author-voice/article-component-router.md` — только когда в тексте
     действительно нужны продуктовые вставки;
   - полный курс — карточка продукта, существующая программа и структурный
     контракт курса; результатом должен быть согласованный набор дней и
     материалов, а не один пост;
   - отдельный материал курса или интенсива — до написания прочитать оболочку
     всего дня, задание и соседние материалы; записать роль материала, уже
     введённые идеи, новый слой и переход дальше;
     если во вводной части курса уже стоит единый обучающий или медицинский
     дисклеймер, не повторять в каждом материале страхующие приписки вроде
     «это не заменяет лечение», «не ставьте себе диагноз» или «обратитесь к
     специалисту». К таким повторам относятся также «это не персональная
     медицинская норма», «это не медицинская граница», «для здорового человека
     это не катастрофа» и автоматическое «обсудите с врачом». Перед выдачей
     учебного материала удалить такие локальные страховки, если они не меняют
     конкретное действие читателя при названном риске. Не считать повторным
     дисклеймером конкретную границу безопасности, которая относится именно к
     содержанию урока: признаки риска, противопоказание или необходимое действие
     читателя сохранять там, где без него совет становится опасным или
     двусмысленным;
   - визуальная оболочка или поля курса — её визуальный контракт; для обычного
     текста урока он не загружается;
   - продажный текст или упоминание продукта —
     `content/author-voice/product-fact-router.md` и найденные через него владельцы
     текущих фактов.
4. Для новой задачи создать приватный JSON по
   `content/author-voice/author-task.schema.json` и явно записать `work_profile`,
   а при известном месте — `surface_context` и площадку в `delivery_platform`
   по карте адаптации. Последнее — редакционная метка, не переключение транспорта.
   В `required_facts` записать только **существенные проверяемые утверждения**:
   цифры и нормы, медицинский риск или безопасность, причинность, актуальные
   условия продукта и любой факт, ошибка в котором изменит совет или основной
   вывод. Разговорное округление, метафору, иронию и явно приблизительную
   формулировку туда не добавлять, если они не меняют фактический смысл. Для
   `required_facts` добавить непустые fingerprints в `fact_sources`; явные
   запреты задачи записать в `forbidden_claims`. Профиль фактчека определяется
   автоматически по площадке: учебный материал проверяется строго, публичный
   пост — по порогу существенной правды. Явный `fact_check_profile` нужен только
   для исключения из этого правила. Использовать канонические площадки:
   `masterclass_material`, `course_material`, `intensive_article` — учебные;
   `telegram_channel`, `pikabu_article`, `site_article` — публичные. Неизвестное
   имя площадки без явного профиля является ошибкой, а не поводом молча выбрать
   мягкую проверку.
   Собрать pack через `tools/author_workflow.py prepare`. Глубина `light`,
   `standard` или `deep`
   выбирается пропорционально объёму и риску задачи; обычный новый пост использует
   `standard`. Для `proofread`, `structure_only`, `targeted_edit` и `text_only`
   retrieval не выполняется: локальный scope и исходный текст уже задают голос.
   Для профиля «Транскрипт → статья» при изменении фраз используется лёгкий
   retrieval только как защита от пересказа и смены голоса; он не даёт права
   заменять речь Сергея новым текстом.
5. В `draft`, `rewrite`, `creative_rebuild`, `targeted_edit` и `text_only`
   применять раздел 10 writer contract уже при написании новых фраз: учитывать,
   что объяснили соседние абзацы, и останавливаться на достаточном объяснении.
   Перед выдачей отдельно перечитать новые фразы по тем же критериям. Не
   откладывать устранение очевидного повтора до будущего аудита; это проверка
   функции, не список запрещённых слов и не разрешение затирать исходник.

## Выполнение

Для большого материала или серии пройти зависимые слои из разделов 11–12
`content/author-voice/writer-contract-v1.md`: полный перенос → сохранность → структура → повторная
оценка → адресная доработка → проверка голоса. Дефект текущего слоя исправляется
до следующего; маленькую однозначную правку можно не дробить.

`full_source` заканчивается чистым целостным текстом без автоматического хвоста.
`sparse_basis` требует полноценного текста и отдельного непубликуемого
owner-review примечания. Для полного курса task дополнительно содержит карту
`course_continuity`; число и объём материалов не выравниваются ради симметрии.

Версионность и отчёт большого прогона вести по
`content/author-voice/writer-contract-v1.md` и
`content/author-voice/material-status-v1.md`: сохранить базу, не перезаписывать показанную Сергею
версию и различать только содержательно значимые слои.

Выполнить выбранный режим по границам выше и content contract из pack. Если для
неоднозначного случая были загружены полные нормативные документы, применить их
уточнения. Площадки и продукты поставляют контекст, поля и факты, но не создают
отдельную личность автора.

При `authored_blocks_first` сначала прочитать
`retrieval.authored_reuse.blocks` и полные тексты из
`retrieval.authored_reuse.sources`, затем выполнить карту переиспользования по
разделу «Сначала авторские блоки, потом новое письмо» из
`content/author-voice/writer-contract-v1.md`. Кандидата с `full_text_in_pack=false` до решения по
блоку открыть точным `item_id` через команду из
`retrieval.authored_reuse.fetch_missing_full_text`; excerpt не считается
достаточным исходником. При `original_composition` старые тексты остаются
материалом для понимания, а не заготовками для автоматической вставки.

При выборе источников ближайшая принятая или опубликованная качественная версия
имеет приоритет над её raw-предками. Полностью читать нужно выбранную основу;
возвращаться к полному транскрипту, голосовому или старой версии — только ради
реального пробела, дополнительного контекста, проверки provenance/точности либо
разрешения конфликта. Это не отменяет `full_source`: запрещено писать по карточке
или excerpt вместо полного текста того источника, на который реально опирается
блок.

До содержательной сборки быстро проверить полный исходник только на уровень 3:
не строится ли материал на ложной или опасной центральной предпосылке. После
любой содержательной сборки — `draft`, `rewrite` или `creative_rebuild` — отдельно
выполнить фактчек всего готового текста по разделу «Три уровня фактического
конфликта» в `content/author-voice/source-integration-pass-v1.md`. Он проверяет и
добавленное Писарем, не становится редактурой, сохраняет собранный авторский
текст и возвращает отдельный отчёт. Полный source-integration pass с поиском,
встройкой ссылок и библиографией выполнять только когда задача действительно
добавляет, переставляет или проверяет доказательные источники.
Уровень 3 останавливает зависимую сборку до черновика, если уже виден в исходнике;
уровни 1–2 не дают права молча менять текст. Любое фактическое исправление после
отчёта выполняется только отдельным разрешённым `targeted_edit`.

После текста запустить `tools/author_workflow.py validate`. Состояние
`needs_fix` требует исправить машинные ошибки. Для `manual_review_required`
закрыть ровно выданные checks через `author_workflow.py review`, указав по каждому
содержательное пояснение, затем повторить validate с review artifact. Только
`pass` является готовым материалом. В любом режиме один и тот же проваленный слой
исправлять автоматически не более двух раз; затем оставить один существенный
блокер, а не продолжать цикл из-за стилистической неточности.

Если Сергей прямо просит опубликовать готовый обычный текстовый материал курса,
не редактировать `content/masterclass/source-current/` и не запускать code deploy.
Найти стабильный `step_id` через
`tools/publish_course_material.py list`, получить текущую версию через `get`, а
после подготовки вызвать `publish` с pack и validation report. Команда публикации
сама проверяет свежий `pass`, использует optimistic locking и создаёт неизменяемую
версию. После ответа API один раз получить материал
через `get` и сверить версию и содержимое. Анкеты, messenger, DQS, офферы,
рецептурные приложения и tutorial этим инструментом не публикуются. Изменение
структуры курса и поддержанных полей карточки через существующий редакционный
публикатор описано в `content/masterclass/editorial/README.md`: эти поля не
становятся code deploy только потому, что не поддерживаются CLI отдельной
статьи. Новый renderer, вид оформления или логика курса остаются разработкой.

### Быстрая публикация из открытого редактора

Если Сергей сохранил готовый текст в открытом боковом редакторе и просит внести
его на сайт, применяется короткий маршрут. Перед публикацией сделать только
быструю корректуру: исправить орфографию, пунктуацию и очевидные опечатки без
переписывания текста. Затем сразу опубликовать сохранённый файл штатным API.

Не сравнивать полный текст с опубликованной версией, не перечитывать материал
заново и не выполнять отдельную содержательную сверку после ответа API. Номер
версии передаётся API только как техническая защита от параллельной перезаписи;
если API вернул конфликт версии, сообщить Сергею и запросить дальнейшее решение.
Для такого маршрута не нужны Git, code deploy и промежуточные ручные загрузки.

Короткий маршрут отменяет повторное содержательное сравнение, не существующий
технический publish gate. Для разрешённой корректуры использовать лёгкий
`develop_existing / proofread`, без голосового retrieval, получить назначенный
`pass` и передать его штатному публикатору. Требование CLI к pack/report не
обходить прямым неподтверждённым PUT; после ответа API полный текст заново не
сравнивать. Это правило относится к отдельной обычной статье, не к публикации
программы или анкеты.

Если Сергей явно попросил работать **автономно**, применить глобальный
`sergey-development-workflow`. Некритичный блокер записать в ledger и перейти к
следующему независимому материалу; остановиться только когда без решения нельзя
безопасно продолжить зависимую часть.

## Проверка и память

Применить только проверки, назначенные выбранным режимом в
`content/author-voice/editing-modes-v1.md` и
`content/author-voice/writer-contract-v1.md`.

Приватная `correction-memory.jsonl` и её SQLite-индекс — общий журнал Писаря,
а не память отдельного чата. Содержательная сборка получает подходящие цепочки
через retrieval pack; устойчивые выводы из них обязаны быть повышены в
канонические правила, чтобы локальная точечная редактура не зависела от истории
конкретной задачи. Не копировать в журнал весь разговор: сохранять различимую
ошибку, замечание владельца, принятую версию, функцию правки и контрграницы.

Существенную правку Сергея сохранить через `tools/record_author_correction.py`
или `tools/record_author_correction_from_codex.py`. Новое устойчивое правило
вносить в единственного владельца соответствующего правила внутри
`content/author-voice/` и связанные машинные доказательства модуля
`platform.content`. Не создавать правила письма в карточках продуктов, курсах,
Telegram-модулях, `work/` или новом паспорте голоса.

После большого прогона применить формат отчёта из
`content/author-voice/material-status-v1.md`:
четыре оси статуса, mini-ledger `применено / записано / отложено`, 3–6 реальных
примеров до/после и редакторская витрина сопоставимых версий. Ответ «понял» не
считается ни внесённой правкой, ни записью в память.

## Редакторская витрина

Фраза Сергея **«покажи редакторскую витрину»** означает: вывести прямо в чат
компактный список рабочих текстовых файлов с понятными названиями и кликабельными
абсолютными локальными ссылками. При нажатии файл должен открываться в правой
редактируемой панели Codex. Материалы группировать по этапам или задачам; несколько
версий одного фрагмента можно дать ссылками через запятую. Полный текст в чат при
этом не копировать, если Сергей отдельно его не попросил.
